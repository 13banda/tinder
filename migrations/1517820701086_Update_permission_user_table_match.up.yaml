- args:
    permission:
      columns:
      - like_user_id
      - hasura_id
      filter:
        hasura_id:
          $eq: X-HASURA-USER-ID
    role: user
    table: match
  type: create_select_permission
